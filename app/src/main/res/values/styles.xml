<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo">
        <!-- Customize your theme here. -->
    </style>

    <style name="SectionHeader" parent="android:Widget.Holo.TextView">
        <item name="android:drawableBottom">@drawable/section_header</item>
        <item name="android:drawablePadding">4dp</item>
        <item name="android:paddingLeft">4dp</item>
        <!--<item name="android:textAllCaps">true</item>-->
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <!--<item name="android:fontFamily">sans-serif-condensed</item>-->
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="RatingBar" parent="@android:style/Widget.Holo.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_bar_full</item>
        <item name="android:minHeight">36dip</item>
        <item name="android:maxHeight">36dip</item>
    </style>

</resources>
